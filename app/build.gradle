apply from: '../application.gradle'

android {
    defaultConfig {
        applicationId "com.excellence.toast.sample"
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    androidTestCompile 'com.android.support.test:runner:1.0.1'
    compile project(':ToastLibrary')
}
